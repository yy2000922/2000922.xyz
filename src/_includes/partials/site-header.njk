<nav class="site-nav">
    <div class="logo">{{ metadata.title }}</div>
    <div class="menu-links">
        {% for item in navigation %}
        <a href="{{ item.url }}" class="{% if page.url == item.url %}active{% endif %}">{{ item.text }}</a>
        {% endfor %}
    </div>
</nav>
