---
layout: layouts/base.njk
title: 标签
permalink: /tags/
---

<h1 class="page-title">标签</h1>
<p class="page-subtitle">使用标签探索更细粒度的内容。</p>

<div style="margin-bottom: 50px; display: flex; flex-wrap: wrap; gap: 15px;">
    {% for tag, posts in collections %}
        {% if tag != "posts" and tag != "all" and tag != "categories" %}
        <a href="#{{ tag }}" class="tag-cloud-link" style="--tag-hue: {{ tag | stringToHue }};">
            #{{ tag }} <span class="tag-cloud-count">{{ posts.length }}</span>
        </a>
        {% endif %}
    {% endfor %}
</div>

<div class="clean-list">
    {% for tag, posts in collections %}
        {% if tag != "posts" and tag != "all" and tag != "categories" %}
        <div id="{{ tag }}" style="margin-bottom: 40px;">
            <h3 class="tag-header" style="--tag-hue: {{ tag | stringToHue }};">#{{ tag }}</h3>
            <div class="clean-list no-border-top" style="border-top: none;">
                {% for post in posts | reverse %}
                <a href="{{ post.url }}" class="clean-item">
                    <span class="clean-item-title">{{ post.data.title }}</span>
                    <span class="clean-item-meta">{{ post.date | readableDate }}</span>
                </a>
                {% endfor %}
            </div>
        </div>
        {% endif %}
    {% endfor %}
</div>
