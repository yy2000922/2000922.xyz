---
layout: layouts/base.njk
title: 标签 | deepwhitex.dev
permalink: /tags/
---

<h1 class="page-title">标签</h1>
<p class="page-subtitle">使用标签探索更细粒度的内容。</p>

<div style="margin-bottom: 50px; display: flex; flex-wrap: wrap; gap: 15px;">
    {% for tag, posts in collections %}
        {% if tag != "posts" and tag != "all" and tag != "categories" %}
        <a href="#{{ tag }}" style="padding: 8px 16px; background: #eee; border-radius: 20px; text-decoration: none; color: var(--text-primary); font-size: 0.9rem; transition: background 0.2s;">
            #{{ tag }} <span style="opacity: 0.5; margin-left: 5px;">{{ posts.length }}</span>
        </a>
        {% endif %}
    {% endfor %}
</div>

<div class="clean-list">
    {% for tag, posts in collections %}
        {% if tag != "posts" and tag != "all" and tag != "categories" %}
        <div id="{{ tag }}" style="margin-bottom: 40px;">
            <h3 style="margin-bottom: 20px; padding-bottom: 10px; border-bottom: 2px solid var(--accent-subtle);">#{{ tag }}</h3>
            <div class="clean-list" style="border-top: none;">
                {% for post in posts | reverse %}
                <a href="{{ post.url }}" class="clean-item">
                    <span class="clean-item-title">{{ post.data.title }}</span>
                    <span class="clean-item-meta">{{ post.date | readableDate }}</span>
                </a>
                {% endfor %}
            </div>
        </div>
        {% endif %}
    {% endfor %}
</div>
